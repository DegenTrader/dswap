import { BitcoinBaseChain } from "./base";
import { BitcoinNetworkConfigMap, BitcoinNetworkInput } from "./utils/types";
export declare class BitcoinCash extends BitcoinBaseChain {
    static chain: "BitcoinCash";
    static configMap: BitcoinNetworkConfigMap;
    configMap: BitcoinNetworkConfigMap;
    static assets: {
        BCH: string;
    };
    assets: {
        BCH: string;
    };
    validateAddress: (address: string) => boolean;
    addressFromBytes: (bytes: Uint8Array) => string;
    addressToBytes: (address: string) => Uint8Array;
    constructor({ network }: {
        network: BitcoinNetworkInput;
    });
}
//# sourceMappingURL=bitcoincash.d.ts.map