import { BitcoinBaseChain } from "./base";
import { BitcoinNetworkConfigMap, BitcoinNetworkInput } from "./utils/types";
/**
 * The Bitcoin class adds support for the asset BTC.
 */
export declare class Bitcoin extends BitcoinBaseChain {
    static chain: "Bitcoin";
    static configMap: BitcoinNetworkConfigMap;
    configMap: BitcoinNetworkConfigMap;
    static assets: {
        BTC: string;
    };
    assets: {
        BTC: string;
    };
    constructor({ network }: {
        network: BitcoinNetworkInput;
    });
}
//# sourceMappingURL=bitcoin.d.ts.map