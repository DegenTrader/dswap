import { BitcoinAPI, UTXO } from "./API";
export declare enum SoChainNetwork {
    BTC = "BTC",
    LTC = "LTC",
    ZEC = "ZEC",
    DOGE = "DOGE",
    DASH = "DASH",
    BTCTEST = "BTCTEST",
    LTCTEST = "LTCTEST",
    ZECTEST = "ZECTEST",
    DOGETEST = "DOGETEST",
    DASHTEST = "DASHTEST"
}
export declare class SoChain implements BitcoinAPI {
    network: string;
    constructor(network?: SoChainNetwork | string);
    fetchHeight: () => Promise<string>;
    fetchUTXO: (txid: string, txindex: string) => Promise<UTXO>;
    fetchUTXOs: (address: string) => Promise<UTXO[]>;
    fetchTXs: (address: string) => Promise<UTXO[]>;
    broadcastTransaction: (txHex: string) => Promise<string>;
}
export interface SoChainUTXO {
    txid: string;
    value: number;
    script_asm: string;
    script_hex: string;
    output_no: number;
    confirmations: number;
    time: number;
}
export interface SoChainTX {
    network: string;
    txid: string;
    blockhash: string | null;
    confirmations: number;
    time: number;
    inputs: Array<{
        input_no: number;
        value: string;
        address: string;
        type: "pubkeyhash";
        script: string;
        witness: null;
        from_output: {
            txid: string;
            output_no: number;
        };
    }>;
    outputs: Array<{
        output_no: 0;
        value: string;
        address: string;
        type: "pubkeyhash";
        script: string;
    }>;
    tx_hex: string;
    size: number;
    version: 1;
    locktime: 0;
}
//# sourceMappingURL=sochain.d.ts.map