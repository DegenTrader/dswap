import { Percent, Token, TokenAmount } from '../entities';
import { NervePool } from './contracts';
import { Symbiosis } from './symbiosis';
export declare class NerveTrade {
    tokenAmountIn: TokenAmount;
    pool: NervePool;
    route: Token[];
    amountOut: TokenAmount;
    callData: string;
    priceImpact: Percent;
    symbiosis: Symbiosis;
    private readonly tokenOut;
    private readonly deadline;
    private readonly slippage;
    constructor(tokenAmountIn: TokenAmount, tokenOut: Token, slippage: number, deadline: number, pool: NervePool, symbiosis: Symbiosis);
    init(): Promise<this>;
}
