import { Percent, Token, TokenAmount } from '../entities';
import { NervePool } from './contracts';
export declare class NerveLiquidity {
    tokenAmountIn: TokenAmount;
    pool: NervePool;
    poolLpToken: Token;
    amountOut: TokenAmount;
    callData: string;
    priceImpact: Percent;
    callDataOffset: number;
    private readonly to;
    private readonly deadline;
    private readonly slippage;
    constructor(tokenAmountIn: TokenAmount, to: string, slippage: number, deadline: number, pool: NervePool);
    init(): Promise<this>;
}
