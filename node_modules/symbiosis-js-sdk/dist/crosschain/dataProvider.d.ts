import { ChainId } from '../constants';
import { Token } from '../entities';
import { OneInchOracle } from './contracts';
import { Symbiosis } from './symbiosis';
export declare class DataProvider {
    private readonly symbiosis;
    private cache;
    constructor(symbiosis: Symbiosis);
    getRepresentation(token: Token, chainId: ChainId): Promise<Token | undefined>;
    getPairs(tokenIn: Token, tokenOut: Token): Promise<import("../entities").Pair[]>;
    getOneInchProtocols(chainId: ChainId): Promise<{
        id: string;
        title: string;
        img: string;
        img_color: string;
    }[]>;
    getOneInchRateToEth(tokens: Token[], oracle: OneInchOracle): Promise<import("./contracts/Multicall").Multicall2.ResultStructOutput[]>;
    private fromCache;
}
