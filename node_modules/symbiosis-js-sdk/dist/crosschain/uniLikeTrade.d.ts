import { Provider } from '@ethersproject/providers';
import { Pair, Percent, Token, TokenAmount, Trade } from '../entities';
import { AdaRouter, AvaxRouter, UniLikeRouter } from './contracts';
import { DataProvider } from './dataProvider';
export declare class UniLikeTrade {
    tokenAmountIn: TokenAmount;
    trade: Trade;
    route: Token[];
    amountOut: TokenAmount;
    callData: string;
    priceImpact: Percent;
    routerAddress: string;
    callDataOffset?: number;
    private pairs;
    private readonly tokenOut;
    private readonly to;
    private readonly deadline;
    private readonly slippage;
    private readonly router;
    private readonly dexFee;
    constructor(tokenAmountIn: TokenAmount, tokenOut: Token, to: string, slippage: number, deadline: number, router: UniLikeRouter | AvaxRouter | AdaRouter, dexFee: number);
    init(dataProvider?: DataProvider): Promise<this>;
    private buildCallData;
    static getPairs(provider: Provider, tokenIn: Token, tokenOut: Token): Promise<Pair[]>;
    private static allPairs;
    private static allPairCombinations;
}
