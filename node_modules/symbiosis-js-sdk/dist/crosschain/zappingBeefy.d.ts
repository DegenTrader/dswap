import { ChainId } from 'src/constants';
import { TokenAmount } from '../entities';
import { SwapExactIn, BaseSwapping } from './baseSwapping';
import { BeefyVault, MulticallRouter } from './contracts';
export declare class ZappingBeefy extends BaseSwapping {
    protected multicallRouter: MulticallRouter;
    protected userAddress: string;
    protected beefyVault: BeefyVault;
    protected aToken: string;
    exactIn(tokenAmountIn: TokenAmount, vaultAddress: string, vaultChainId: ChainId, from: string, to: string, revertableAddress: string, slippage: number, deadline: number, use1Inch?: boolean): SwapExactIn;
    protected finalReceiveSide(): string;
    protected finalCalldata(): string | [];
    protected finalOffset(): number;
    protected swapTokens(): string[];
    private buildMulticall;
}
