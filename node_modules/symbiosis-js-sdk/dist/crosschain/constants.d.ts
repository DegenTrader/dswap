import JSBI from 'jsbi';
import { ChainId } from '../constants';
import { Percent, Token } from '../entities';
export declare const CHAINS_PRIORITY: ChainId[];
declare type ChainTokensList = {
    readonly [chainId in ChainId]?: Token[];
};
export declare const ONE_INCH_CHAINS: ChainId[];
export declare const ONE_INCH_ORACLE_MAP: {
    [chainId in ChainId]?: string;
};
export declare const WETH_ONLY: ChainTokensList;
export declare const DEX_TOKENS_TO_CHECK_TRADES_AGAINST: {
    1: Token[];
    56: Token[];
    43114: Token[];
    137: Token[];
    288: Token[];
    2001: Token[];
    1313161554: Token[];
};
export declare const BASES_TO_CHECK_TRADES_AGAINST: ChainTokensList;
/**
 * Some tokens can only be swapped via certain pairs, so we override the list of bases that are considered for these
 * tokens.
 */
export declare const CUSTOM_BASES: {
    [chainId in ChainId]?: {
        [tokenAddress: string]: Token[];
    };
};
export declare const ONE_BIPS: Percent;
export declare const BIPS_BASE: JSBI;
export declare const MULTICALL_ADDRESSES: {
    [chainId in ChainId]?: string;
};
export {};
