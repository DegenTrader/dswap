import { TradeType } from "../../../..";
import { InstantTrade } from "../../../instant-trade";
import { PriceToken } from "../../../../../core";
import { PriceTokenAmount } from "../../../../../core/blockchain/tokens/price-token-amount";
import { LifiCalculationOptions } from "./models/lifi-calculation-options";
export declare class LifiProvider {
    private readonly lifi;
    private readonly defaultOptions;
    constructor();
    calculate(from: PriceTokenAmount, toToken: PriceToken, disabledProviders: TradeType[], options?: LifiCalculationOptions): Promise<InstantTrade[]>;
    private getGasFeeInfo;
    private getPath;
}
