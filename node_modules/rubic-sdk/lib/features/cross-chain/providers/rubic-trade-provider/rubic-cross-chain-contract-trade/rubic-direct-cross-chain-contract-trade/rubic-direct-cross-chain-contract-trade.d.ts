import { PriceTokenAmount } from "../../../../../../core/blockchain/tokens/price-token-amount";
import BigNumber from 'bignumber.js';
import { CrossChainContractData } from "../../../common/celer-rubic/cross-chain-contract-data";
import { RubicCrossChainContractTrade } from "../rubic-cross-chain-contract-trade";
import { RubicCrossChainSupportedBlockchain } from "../../constants/rubic-cross-chain-supported-blockchains";
export declare class RubicDirectCrossChainContractTrade extends RubicCrossChainContractTrade {
    private readonly token;
    readonly fromToken: PriceTokenAmount;
    readonly toToken: PriceTokenAmount;
    readonly toTokenAmountMin: BigNumber;
    constructor(blockchain: RubicCrossChainSupportedBlockchain, contract: CrossChainContractData, token: PriceTokenAmount);
    protected getFirstPath(): string[];
    getSecondPath(): string[];
    protected modifyArgumentsForProvider(methodArguments: unknown[][]): Promise<void>;
}
