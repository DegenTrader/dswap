import { BlockchainName, PriceTokenAmount } from "../../../../../../core";
import { CrossChainContractData } from "../../../common/celer-rubic/cross-chain-contract-data";
import { RubicCrossChainContractTrade } from "../rubic-cross-chain-contract-trade";
import { RubicCrossChainSupportedInstantTrade } from "./rubic-cross-chain-instant-trade/rubic-cross-chain-supported-instant-trade";
import BigNumber from 'bignumber.js';
export declare class RubicItCrossChainContractTrade extends RubicCrossChainContractTrade {
    readonly slippage: number;
    private readonly instantTrade;
    readonly fromToken: PriceTokenAmount;
    readonly toToken: PriceTokenAmount;
    readonly toTokenAmountMin: BigNumber;
    private readonly crossChainInstantTrade;
    constructor(blockchain: BlockchainName, contract: CrossChainContractData, providerIndex: number, slippage: number, instantTrade: RubicCrossChainSupportedInstantTrade);
    protected getFirstPath(): string[] | string;
    getSecondPath(): string[];
    protected modifyArgumentsForProvider(methodArguments: unknown[][], walletAddress: string, swapTokenWithFee: boolean): Promise<void>;
    private getTrade;
}
