import { Blockchain } from "./models/blockchain";
import { BlockchainName } from "./models/blockchain-name";
/**
 * Works with list of all used in project blockchains.
 * Contains method to find info about certain blockchain.
 */
export declare class BlockchainsInfo {
    /**
     * An array of all blockchains, used in project.
     */
    static readonly blockchains: ReadonlyArray<Blockchain>;
    /**
     * Finds blockchain object, based on provided chain id.
     */
    static getBlockchainById(chainId: string | number): Blockchain | undefined;
    /**
     * Finds blockchain object, based on provided blockchain name.
     */
    static getBlockchainByName(blockchainName: BlockchainName): Blockchain;
}
